import { CommonDialog } from "../../../../../components/dialog/CommonDialog";

export const MapBodyPreviewDialog = ({ isOpen }) => {
  return <CommonDialog isOpen={isOpen} content={<>'hello'</>} />;
};
